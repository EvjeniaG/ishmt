/** Regjistrimi lexon bashkitë nga DB — mos e prerender-o gjatë build-it pa DATABASE_URL. */
export const dynamic = "force-dynamic";

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return children;
}
